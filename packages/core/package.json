{
  "name": "@littlehands/core",
  "version": "0.1.0",
  "private": true,
  "description": "Platform-independent domain logic for Little Hands: tracing engine, puzzles, rewards, screen time, recommendation, parental gate, content-pack schema. No React Native dependencies - runs in Node, mobile and (future) web.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "jest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.5.4"
  }
}
